# remember-api API runtime env / API 服务运行时环境变量
#
# Deployment has two separate phases:
# 部署分两阶段：
#
# 1) One-time database init: run migrations + seed a Profile/API key.
#    一次性初始化数据库：跑迁移 + seed 写入 Profile/API key。
# 2) Long-running API service: start the gateway process.
#    长驻 API 服务：启动网关进程。
#
# This file is for phase 2: the long-running API process.
# 本文件主要给第 2 阶段使用：API 长驻进程。
# After the database has been migrated and seeded, the API deployment usually only needs:
# 数据库已经迁移并 seed 后，API 部署通常只需要：
#
#   DATABASE_URL
#   DATABASE_SSL_CA_PEM
#   ENCRYPTION_KEY
#   UPSTREAM_API_KEY
#
# The API process does NOT choose provider/model from env. It reads the requested Profile
# from the database on every request. The default seed creates Profile "default" with:
# API 长驻进程不从环境变量选择 provider/model，而是每次请求读取数据库里的 Profile。
# 默认 seed 会创建名为 "default" 的 Profile：
#   provider = deepseek
#   model    = deepseek-chat
#
# To choose another bottom model for a fresh database, pass SEED_PROVIDER/SEED_MODEL to
# the one-time seed command. Do not put them in the API deployment env and expect them
# to override an existing Profile.
# 如果新库要用别的底层模型，请在一次性 seed 命令里传 SEED_PROVIDER/SEED_MODEL。
# 不要把它们填进 API 部署环境变量后期待覆盖已有 Profile。

# ── Database / 数据库 ───────────────────────────────────────
# Runtime database connection string / API 运行时数据库连接串。
# Supabase example: transaction pooler on port 6543.
# Supabase 示例：事务池端口 6543。
# Username must be postgres.<ref>, not plain postgres.
# 用户名必须是 postgres.<ref>，不是裸 postgres。
DATABASE_URL=postgres://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&sslmode=require

# PEM contents of the DB root CA / 数据库根 CA 的 PEM 内容。
# Supabase: paste the full prod-ca-2021.crt contents, including BEGIN/END lines.
# Supabase：粘贴 prod-ca-2021.crt 全文，包括 BEGIN/END 两行。
# Leave empty for local PostgreSQL or providers using public CA certificates.
# 本机 PostgreSQL 或公开 CA 签发的数据库可留空。
DATABASE_SSL_CA_PEM=

# ── Upstream model / 上游模型 ───────────────────────────────
# Upstream provider key / 上游模型厂商 Key。
# This key is used only after the requested Profile decides which provider/model to call.
# The default seeded Profile calls deepseek / deepseek-chat, so use a DeepSeek key unless
# you seeded or edited the Profile differently.
# 这把 Key 只是在 Profile 决定 provider/model 之后被拿来调用上游。
# 默认种子 Profile 调用 deepseek / deepseek-chat；除非你 seed 或修改过 Profile，
# 否则这里应填 DeepSeek Key。
UPSTREAM_API_KEY=sk-your-deepseek-key

# ── Fresh DB seed example / 新数据库 seed 示例 ───────────────
# Do NOT set these on the deployed API service. Run them once in the shell that executes
# `pnpm db:seed`, together with DATABASE_URL and API_KEY_PEPPER if you customized it.
# 不要把这些变量配置到已部署的 API 服务上。只在执行 `pnpm db:seed` 的那个 shell 里一次性传入，
# 并同时传 DATABASE_URL；如果自定义了 API_KEY_PEPPER，也要传同一个值。
#
# DeepSeek example / DeepSeek 示例：
#   DATABASE_URL=... \
#   API_KEY_PEPPER=change-me-in-production \
#   BOOTSTRAP_API_KEY=rma_bootstrap_local \
#   SEED_PROVIDER=deepseek \
#   SEED_MODEL=deepseek-chat \
#   pnpm db:seed
#
# OpenAI example / OpenAI 示例：
#   DATABASE_URL=... \
#   API_KEY_PEPPER=change-me-in-production \
#   BOOTSTRAP_API_KEY=rma_bootstrap_local \
#   SEED_PROVIDER=openai \
#   SEED_MODEL=gpt-4o-mini \
#   pnpm db:seed
#
# If the Profile already exists, db:seed is idempotent and will not rewrite provider/model.
# To change an existing deployment, update the Profile in the database/CLI.
# 如果 Profile 已存在，db:seed 是幂等的，不会重写 provider/model。
# 已部署后要换模型，请通过数据库/CLI 修改 Profile。

# ── Encryption / 加密 ───────────────────────────────────────
# Encryption key for provider_configs.api_key_encrypted.
# provider_configs.api_key_encrypted 的加密密钥。
# If you only use UPSTREAM_API_KEY, this is rarely touched, but keep one stable value in deployment.
# 如果只走 UPSTREAM_API_KEY，通常不会读到它；部署环境里保留一个稳定值即可。
# Do not rotate it after storing provider keys in the database.
# 一旦库里存过加密 provider key，就不要随便更换。
ENCRYPTION_KEY=change-me-in-production

# ── Optional runtime overrides / 可选运行时覆盖 ──────────────
# Keep empty for DeepSeek: the provider default is already https://api.deepseek.com.
# DeepSeek 正常留空：代码内置默认端点已经是 https://api.deepseek.com。
# Set only for proxies, private gateways, or custom OpenAI-compatible endpoints.
# 只有代理、私有网关、自定义 OpenAI 兼容地址才填写。
UPSTREAM_BASE_URL=

# API Key hash pepper / API Key 哈希 pepper。
# Only set this if the seed command used the same custom value.
# 只有 seed 命令也用了同一个自定义值时才设置。
# If omitted, runtime uses the built-in default; mismatching seed/runtime values cause 401.
# 不填则运行时用内置默认值；seed 与运行时不一致会导致 401。
# API_KEY_PEPPER=change-me-in-production

# Optional memory backend / 可选记忆后端。
# Empty = built-in DB memory provider. Set only when running Mem0 bridge.
# 留空 = 内置 DB memory provider。只有运行 Mem0 bridge 时才设置。
# MEM0_BASE_URL=
# MEM0_API_KEY=

# Optional server settings / 可选服务设置。
# Most platforms inject PORT automatically; only set these when you need to override them.
# 多数平台会自动注入 PORT；只有需要覆盖时才设置。
# PORT=4000
# HOST=0.0.0.0
# LOG_LEVEL=info
